496. Next Greater Element I
class Solution:
    def nextGreaterElement(self, nums1: List[int], nums2: List[int]) -> List[int]:
        s=[]
        dic={}
        for num in nums2:
            while s and s[-1]<num:
                dic[s.pop()]=num
            s.append(num)
        ans=[]
        for n in nums1:
            if n in dic:
                ans.append(dic[n])
            else:
                ans.append(-1)
        return ans
